<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Chat App" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Chat App" %>">
    <%= stylesheet_link_tag 'calendar', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'calendar', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%#= render 'layouts/navigation' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
  </body>
</html>
